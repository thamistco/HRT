{
  "name": "find-your-hrt-build-tools",
  "private": true,
  "description": "Dev-only build tooling for this static site. Not required at runtime — visitors never download anything from here.",
  "scripts": {
    "build": "node scripts/build-tool.js"
  },
  "devDependencies": {
    "terser": "^5.36.0"
  }
}
